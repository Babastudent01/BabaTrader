# Core
python-dotenv>=1.0.0
pyyaml>=6.0.1
pandas>=2.2.0
numpy>=1.26.0
pytz>=2024.1

# Data
yfinance>=0.2.38
requests>=2.31.0
requests-cache>=1.2.0

# Technical indicators
pandas-ta>=0.3.14b

# Data serialisation (cache)
pyarrow>=14.0.0

# Machine Learning
scikit-learn>=1.4.0
xgboost>=2.0.3
lightgbm>=4.3.0
joblib>=1.3.2

# Plotting / Reporting
matplotlib>=3.8.0
seaborn>=0.13.2

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Optional: Interactive Brokers (uncomment when ready)
# ibapi>=9.81.1.post1

# Optional: Saxo Bank (uncomment when ready)
# saxo-openapi>=0.0.9

# News / Sentiment
newsapi-python>=0.2.7
vaderSentiment>=3.3.2

# Calendar / Holidays
holidays>=0.50

# Deep learning — GPU backend is auto-detected at runtime (CUDA → DirectML → MPS → CPU)
#
# CPU build (current install):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# NVIDIA GPU (CUDA 12.x):
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
#
# AMD GPU on Windows (7800 XT / RDNA 3 — DirectML):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install torch-directml
#
# AMD GPU on Linux (ROCm 6.2):
#   pip install torch --index-url https://download.pytorch.org/whl/rocm6.2
#
torch>=2.2.0
torch-directml   # Uncomment for AMD GPU on Windows

# Optional: Deep-learning sentiment (uncomment when ready)
# transformers>=4.38.0
